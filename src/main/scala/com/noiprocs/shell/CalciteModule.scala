package com.noiprocs.shell

import com.google.inject.AbstractModule
import net.codingwell.scalaguice.ScalaModule

class CalciteModule extends AbstractModule with ScalaModule {
  override def configure(): Unit = {}
}
